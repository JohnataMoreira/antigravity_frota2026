# .env.production.example - Frota2026 Production Configuration

# -----------------------------------------------------------------------------
# DATABASE
# -----------------------------------------------------------------------------
# Replace 'StrongPassword' with a secure random string
DB_PASSWORD=SecurePassword123!
DATABASE_URL="postgresql://postgres:${DB_PASSWORD}@postgres:5432/frota2026?schema=public"

# -----------------------------------------------------------------------------
# SECURITY
# -----------------------------------------------------------------------------
# Generate using: openssl rand -base64 32
JWT_SECRET=YourGeneratedStrongBase64SecretHere

# -----------------------------------------------------------------------------
# STORAGE (Internal MinIO)
# -----------------------------------------------------------------------------
STORAGE_ENDPOINT=http://minio:9000
STORAGE_BUCKET=frota-uploads
STORAGE_REGION=us-east-1
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin
AWS_S3_FORCE_PATH_STYLE=true

# -----------------------------------------------------------------------------
# APP CONFIG
# -----------------------------------------------------------------------------
PORT=3000
VITE_API_URL=https://frota.johnatamoreira.com.br/api
NODE_ENV=production
